/**
 * Script pour gérer l'authentification dans l'interface
 * Ce script a été simplifié pour ne plus afficher de badge dans l'entête
 */

document.addEventListener('DOMContentLoaded', function() {
    // Code désactivé pour supprimer le badge "Connexion" en haut à droite
    // comme demandé par le client
});